suites:
  jwt_auth_suite:
    namespace: Tymon
    src_path: src
    spec_prefix: spec
extensions:
  - PhpSpec\Extension\CodeCoverageExtension
code_coverage:
  format:
    - clover
  output:
    clover: coverage.xml