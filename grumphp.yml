parameters:
  git_dir: .
  bin_dir: ./vendor/bin
  tasks:
    composer:
      metadata:
        priority: 600
    jsonlint:
      metadata:
        priority: 500
    xmllint:
      metadata:
        priority: 400
    yamllint:
      metadata:
        priority: 300
    securitychecker:
      metadata:
        priority: 200
    phpunit:
      metadata:
        priority: 100
